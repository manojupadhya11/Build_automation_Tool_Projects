services:
  projectops-python:
    build: .
    container_name: projectops-python
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - HOST=0.0.0.0
      - PORT=5000
      - DB_PATH=data/devopsshack.db
